const name="rory"
const repocount=20
console.log(name+repocount)
console.log(`hello mt name is ${name} and myrepo count is ${repocount}`)
const place=new String('delhi')
console.log(place.toUpperCase())
//console.log(place.charAt(e)) aise e nhi likh slte
  console.log(place.charAt(2))

  const newplace=place.substring(0,2)
  console.log(newplace)